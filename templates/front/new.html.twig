{% extends 'base.html.twig' %}

{% block title %} Nouveau rapport {% endblock %}

{% block body %}
    <div class="container">
        <h1 class="text-center mb-5 text-uppercase">| NOUVEAU RAPPORT SCP |</h1>
        {{ form_start(form) }}
            <div class="mb-3 text-center">TITRE</div>
            <div class="text-end">{{ form_errors(form.title) }}</div>
            <div class="mb-3">{{ form_widget(form.title) }}</div>
            <div class="mb-3 text-center">NUMÉRO</div>
            <div class="text-end">{{ form_errors(form.number) }}</div>
            <div class="mb-3">{{ form_widget(form.number) }}</div>
            <div class="mb-3 text-center">CONTENU DU RAPPORT</div>
            <div class="text-end">{{ form_errors(form.content) }}</div>
            <div class="mb-3">{{ form_widget(form.content) }}</div>
            <div class="mb-3 text-center">MENACES</div>
            <div class="mb-3">{{ form_widget(form.menace) }}</div>
            <div class="mb-3 text-center">IMAGE</div>
            <div class="text-end">{{ form_errors(form.image) }}</div>
            <div class="mb-3">{{ form_widget(form.image) }}</div>
            <div class="d-flex justify-content-center"><button class="btn btn-outline-light mt-3" type='submit'>ENREGISTRER</button></div>
        {{ form_end(form) }}
    </div>
{% endblock %}
