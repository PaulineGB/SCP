{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<div class="container">
    <h1 class="text-center mt-3 mb-5 text-uppercase">| MON COMPTE |</h1>
    <table class="table" style="color: white;">
        <tbody>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            {# <tr>
                <th>Mot de passe</th>
                <td>{{ user.password }}</td>
            </tr> #}
            <tr>
                <th>Pseudo</th>
                <td>{{ user.pseudo }}</td>
            </tr>
        </tbody>
    </table>
    {# <div class="d-flex justify-content-center"><button class="btn btn-outline-light mt-3"><a class="text-reset text-decoration-none" href="{{ path('user_edit', {'id': user.id}) }}">MODIFIER LES INFOS</a></button></div> #}
</div>
{% endblock %}
