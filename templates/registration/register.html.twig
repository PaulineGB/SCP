{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div class="container text-center">
        <h1 class="h3 mb-5 font-weight-normal" id="connexion">| ACCRÉDITATION |</h1>

        {{ form_start(registrationForm) }}
            <div class="mb-3 text-center">EMAIL</div>
            <div class="text-center">{{ form_errors(registrationForm.email) }}</div>
            <div class="mt-3">{{ form_widget(registrationForm.email) }}</div>
            <div class="mb-3 text-center mt-3">PSEUDO</div>
            <div class="text-center">{{ form_errors(registrationForm.pseudo) }}</div>
            <div class="mt-3">{{ form_widget(registrationForm.pseudo) }}</div>
            <div class="mb-3 text-center mt-3">MOT DE PASSE</div>
            <div class="text-center">{{ form_errors(registrationForm.plainPassword) }}</div>
            <div class="mt-3">{{ form_widget(registrationForm.plainPassword) }}</div>
            <div class="mt-5"><p>Accepter les conditions d'utilisation {{ form_widget(registrationForm.agreeTerms, {label: 'Accepter les conditions d\'utilisation'}) }}</p></div>

            <div class="d-flex justify-content-center"><button class="btn btn-outline-light mt-3" type='submit'>ENREGISTRER</button></div>
        {{ form_end(registrationForm) }}
    </div>
{% endblock %}
